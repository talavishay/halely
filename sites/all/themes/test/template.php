<?php

/**
 * @file
 * Template overrides as well as (pre-)process and alter hooks for the
 * test theme.
 */
function test_omega_layout_alter(&$layout) {
/*
    if (arg(0) == 'node' && is_numeric(arg(1))) {
      $nid = arg(1);
      $node = node_load($nid);
      if (isset($node) && $node->type == 'page') {*/
/*

        $layout = 'Simple';*/

/*
    }   
*/	
}